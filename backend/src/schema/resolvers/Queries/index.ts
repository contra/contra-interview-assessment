import { 
    queryFlag as flag, 
    queryFlags as flags 
} from './flags';
import { 
  queryUser as user, 
  queryUsers as users 
} from './users';

export const Query = {
  flag,
  flags,
  user,
  users,
};
