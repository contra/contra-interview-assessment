import { featureFlags, featureFlag } from './featureFlag';
import { users, user, usersWithFeatureFlags } from './user';

export const Query = {
  featureFlag,
  featureFlags,
  user,
  users,
  usersWithFeatureFlags,
};
