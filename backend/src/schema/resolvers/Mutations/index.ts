import { resolve as setUserFeatureFlag } from './setUserFeatureFlag';
import { resolve as setFeatureFlag } from './setFeatureFlag';

export const Mutation = {
  setUserFeatureFlag,
  setFeatureFlag
};
