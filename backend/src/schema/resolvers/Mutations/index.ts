import { resolve as sampleMutation } from './sampleMutation';
import { resolve as setFlagForUsers } from './setFlagForUsers';
import { resolve as updateFlagForUser } from './updateFlagForUser';

export const Mutation = {
  sampleMutation,
  setFlagForUsers,
  updateFlagForUser,
};
