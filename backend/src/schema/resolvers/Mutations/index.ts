import { resolve as updateFeatureFlags } from './updateFeatureFlags';
import { resolve as updateUserAccounts } from './updateUserAccounts';

export const Mutation = {
  updateFeatureFlags,
  updateUserAccounts,
};
