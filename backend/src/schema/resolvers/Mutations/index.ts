import { resolve as sampleMutation } from './sampleMutation';
import { resolve as assignFlag } from './assignFlag';
import { resolve as changeFlag } from './changeFlag';

export const Mutation = {
  sampleMutation,
  assignFlag,
  changeFlag
};
