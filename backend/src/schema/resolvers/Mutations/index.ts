import { resolve as sampleMutation } from './sampleMutation';
import { resolve as setUsersFeatureFlag } from './setUsersFeatureFlagMutation';
import { resolve as removeUsersFeatureFlag } from './removeUsersFeatureFlagMutation';

export const Mutation = {
  sampleMutation,
  setUsersFeatureFlag,
  removeUsersFeatureFlag,
};
