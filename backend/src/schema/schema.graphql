type Mutation {
  sampleMutation: String!
}

type Query {
  hello: String!
  featureFlags: [FeatureFlag!]
}

type FeatureFlag {
  flagKey: String!
  flagType: String!
  defaultValue: String
}

type FeatureFlagUserValue {
  flagKey: String!
  flagType: String!
  flagValue: String!
}
